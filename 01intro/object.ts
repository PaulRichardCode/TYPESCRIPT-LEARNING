let john = {
  name: "saheed",
  class: 2,
  human: true,
};

// let sad = ({ name: string, human: boolean }) => {};

// sad({ name: 123, human: "daddy" });

//***** Difference between let and type is type let you define and use the variable type in an objects

type Bingo = {
  name: string;
  human: boolean;
};
